---
name: Neon Postgres Egress Optimizer
description: Diagnose and fix excessive Postgres egress (network data transfer) in a codebase. Use when a user mentions high database bills, unexpected data transfer costs, network transfer charges, egress spikes, "why is my Neon bill so high", "database costs jumped", SELECT * optimization, query...
frameworks: [Database, neon, postgres, egress, optimizer, AAS Core, Agentic]
---

# Neon Postgres Egress Optimizer

Diagnose and fix excessive Postgres egress (network data transfer) in a codebase. Use when a user mentions high database bills, unexpected data transfer costs, network transfer charges, egress spikes, "why is my Neon bill so high", "database costs jumped", SELECT * optimization, query...

## Category & Classification
- **Domain**: Database
- **Risk Profile**: `critical`
- **Source**: https://github.com/neondatabase/agent-skills/tree/main/skills/neon-postgres-egress-optimizer
- **Triggers**: `neon`, `postgres`, `egress`, `optimizer`, `diagnose`, `fix`, `excessive`, `network`

## Usage & Execution Guidelines
1. **Context Activation**: Invoke this skill when dealing with neon postgres egress optimizer tasks or related sub-problems.
2. **Rule Enforcement**: Follow industrial best practices, keep implementations modular, and ensure complete type-safety.
3. **Verification**: Run comprehensive diagnostics and edge-case unit tests before marking task as complete.

## Supported Agent Harnesses
- **Claude Code**: `/neon-postgres-egress-optimizer`
- **Antigravity / OpenAI Codex**: `.agents/skills/neon-postgres-egress-optimizer/SKILL.md`
- **Cursor**: `.cursor/rules/neon-postgres-egress-optimizer.mdc`

