---
name: TDD Workflow
description: Complete test-driven development workflow: plan â†’ RED (failing test) â†’ GREEN (minimal impl) â†’ REFACTOR â†’ verify.
frameworks: [Claude Code, Codex, Cursor, Antigravity]
---

# TDD Workflow

Complete test-driven development workflow: plan â†’ RED (failing test) â†’ GREEN (minimal impl) â†’ REFACTOR â†’ verify.

## Use Case
Building any feature where correctness is critical. Start with a failing test, implement the minimum to pass, then clean up.

## Supported Platforms
- Claude Code
- Codex
- Cursor
- Antigravity
- Zed
- Copilot

## Core Engineering Rules
1. Apply the **TDD Workflow** pattern systematically before making code changes.
2. Validate outputs across supported AI harnesses (Claude Code, Codex, Antigravity, Cursor).
3. Prevent context pollution and maintain strict verification standards.
